# 🎀 Mochi CLI 🎀
**Version 2025.10.26**

A tiny, cross-platform package fetcher & transfer tool —
built with **Python + Flask**, wrapped in a pastel shell 💖

Mochi lets you **host**, **list**, and **fetch** files (like a mini Chocolatey/Scoop for **Linux**, **Windows**, and **macOS**).
Perfect for quickly sharing builds, scripts, or archives **with integrity checks (SHA1)**.

---

## ✨ Features
| Icon | Feature |
|------|---------|
| 🌸 | **Lightweight** Python + Flask architecture |
| 🎀 | **Cross-platform** client & server |
| 💾 | **SHA1 hash validation** for safe transfers |
| 🌈 | **Cute pastel-colored** terminal interface |
| 🔒 | **Token-based authentication** (optional, fully implemented) |
| 🔐 | **SSL/TLS encryption** (optional, fully implemented) |

---

## ⚙️ Usage

### 🧁 Client (Mochi CLI)
```bash
mochi touch                 # Ping server
mochi list                  # List packages
mochi fetch PACKAGE         # Fetch a package by name
```
